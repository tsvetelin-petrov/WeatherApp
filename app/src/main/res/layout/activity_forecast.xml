<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".activity.ForecastActivity">

    <SearchView
        android:id="@+id/searchNameForecast"
        android:layout_width="match_parent"
        android:layout_height="30dp" />

    <Button
        android:id="@+id/changeToWeather"
        android:layout_height="wrap_content"
        android:layout_width="match_parent"
        android:layout_below="@id/searchNameForecast"
        android:layout_centerVertical="true"
        android:text="@string/change_to_weather" />

    <TextView
        android:id="@+id/cityTextForecast"
        style="?android:attr/textAppearanceMedium"
        android:layout_width="match_parent"
        android:textAlignment="center"
        android:layout_height="wrap_content"
        android:layout_below="@id/changeToWeather"
        android:layout_centerHorizontal="true" />

    <ListView
        android:id="@+id/weatherList"
        style="@style/weatherListStyle"
        android:layout_width="match_parent"
        android:layout_below="@id/cityTextForecast"
        android:layout_height="match_parent"
        android:layout_marginTop="15dp" />

</RelativeLayout>